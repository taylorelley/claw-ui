# Development environment (Vite dev server)
# When running `npm run dev`, the WebSocket needs to connect to the plugin server

# Connect to OpenClaw plugin server
VITE_CLAW_WS_URL=ws://localhost:18800/ws

# Cloud relay URL for setup wizard
VITE_RELAY_URL=wss://claw-ui-relay-dev.app.taylorelley.com/relay

# Auth token (change this to match your OpenClaw config)
VITE_CLAW_AUTH_TOKEN=dev-token-123
