# Production environment (served by the plugin)
# Use same-origin /ws endpoint when served by the OpenClaw plugin

# Empty URL = use same-origin /ws (recommended for production)
VITE_CLAW_WS_URL=

# Cloud relay URL for setup wizard
VITE_RELAY_URL=wss://claw-ui.app.taylorelley.com/relay

# Auth token should match OpenClaw config
# This will be baked into the build, so use a strong token
VITE_CLAW_AUTH_TOKEN=
