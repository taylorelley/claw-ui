# Claw UI Environment Configuration

# WebSocket URL for OpenClaw channel plugin
# Leave empty to use same-origin /ws endpoint (recommended when served by the plugin)
VITE_CLAW_WS_URL=

# Authentication token (must match the token in OpenClaw config)
# Leave empty if auth.mode is set to "none" in OpenClaw config
VITE_CLAW_AUTH_TOKEN=your-secret-token

# Supabase configuration (for session persistence and adaptive engine)
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
